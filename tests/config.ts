export const MOCK_PORT = 8001;
export const API_BASE = `http://localhost:${MOCK_PORT}/api/alpha`;
export const GHOST_BASE = `http://localhost:${MOCK_PORT}`;
export const APP_URL = 'http://localhost:3000';
